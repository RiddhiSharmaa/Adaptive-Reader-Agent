# Adaptive Reader Agent

AI reading companion that builds a confidence-scored, evolving model
of a reader's preferences (pacing, tropes, mood, tone) instead of
relying on genre-tag matching.

Built for AI.Accelerate FY26, Path B capstone.

## Architecture
See /docs for the RAG knowledge base and proposal.